# 状态效果 v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 GDScript 战斗骨架中加入第一版可测试状态效果系统，并让策划调试场景显示玩家和敌人的状态效果。

**Architecture:** 以 `scripts/stm/powers/` 作为状态效果边界，`StmCreature` 只暴露添加、查询、结算、回合通知等通用接口。攻击、格挡、施加状态仍通过现有 Action Queue 结算，卡牌只产出动作，调试场景只读取战斗状态并刷新 UI。

**Tech Stack:** Godot 4.6.2、GDScript、GUT、现有 `StmCombatActions` / `StmCombat` / `StmCard` / `StmCreature` 架构。

---

## 约束和边界

- 只修改 `C:\Users\User\Documents\GitHub\Godot-Game` 内的 Godot 项目。
- 不修改 `slay-the-model-main/` Python 参考项目。
- 不引入消息总线、正式卡牌库、正式敌人库、资源化内容编辑器。
- 本阶段只实现测试用状态效果和测试卡：`力量`、`敏捷`、`虚弱`、`易伤`、`Bash`、`Inflame`、`Shrug It Off`。
- 测试函数名保留英文，避免 GDScript 标识符兼容风险；测试内 Given-When-Then 行为注释必须使用中文。
- 运行所有单元测试使用：

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

## 文件结构

**Create**
- `scripts/stm/powers/power.gd`：状态效果基类，负责堆叠、修正钩子、持续时间、展示文本。
- `scripts/stm/powers/strength.gd`：力量，提高攻击伤害。
- `scripts/stm/powers/dexterity.gd`：敏捷，提高获得格挡。
- `scripts/stm/powers/weak.gd`：虚弱，降低造成伤害。
- `scripts/stm/powers/vulnerable.gd`：易伤，提高受到伤害。
- `scripts/stm/cards/test/bash.gd`：测试攻击牌，造成伤害并施加易伤。
- `scripts/stm/cards/test/inflame.gd`：测试能力牌，给玩家施加力量。
- `scripts/stm/cards/test/shrug_it_off.gd`：测试技能牌，获得格挡并抽牌。
- `scripts/stm/tests/test_powers_v1.gd`：状态效果 v1 的中文 BDD 测试。

**Modify**
- `scripts/stm/entities/creature.gd`：新增状态效果容器 API 和数值修正入口。
- `scripts/stm/actions/combat_actions.gd`：攻击、格挡动作接入状态修正；新增 `ApplyPowerAction`。
- `scripts/stm/cards/card.gd`：基础卡牌出牌时把 `self` 传入攻击和格挡动作。
- `scripts/stm/engine/combat.gd`：在回合边界通知状态效果。
- `scripts/stm/debug/battle_debug_scene.gd`：新增玩家和敌人状态效果显示。
- `scripts/stm/tests/test_battle_debug_scene.gd`：补充调试场景状态效果展示测试。
- `.gutconfig.json`：加入 `test_powers_v1.gd`。

## Task 1: 状态效果基类与生物状态容器

**Files:**
- Create: `scripts/stm/powers/power.gd`
- Create: `scripts/stm/powers/strength.gd`
- Create: `scripts/stm/powers/vulnerable.gd`
- Modify: `scripts/stm/entities/creature.gd`
- Test: `scripts/stm/tests/test_powers_v1.gd`
- Modify: `.gutconfig.json`

- [ ] **Step 1: Write the failing test**

Add `scripts/stm/tests/test_powers_v1.gd` with these first tests:

```gdscript
extends GutTest

const PlayerScript := preload("res://scripts/stm/player/player.gd")
const EnemyScript := preload("res://scripts/stm/enemies/enemy.gd")
const CombatActionsScript := preload("res://scripts/stm/actions/combat_actions.gd")
const StrengthScript := preload("res://scripts/stm/powers/strength.gd")
const VulnerableScript := preload("res://scripts/stm/powers/vulnerable.gd")


func test_apply_power_stacks_intensity_or_duration() -> void:
	# Given：玩家和敌人分别准备接收强度型状态与持续型状态。
	var player = PlayerScript.new([])
	var enemy = EnemyScript.new(20, "测试敌人", 0)
	var apply_strength = CombatActionsScript.ApplyPowerAction.new(player, StrengthScript.new(2))
	var apply_more_strength = CombatActionsScript.ApplyPowerAction.new(player, StrengthScript.new(3))
	var apply_vulnerable = CombatActionsScript.ApplyPowerAction.new(enemy, VulnerableScript.new(2))
	var apply_more_vulnerable = CombatActionsScript.ApplyPowerAction.new(enemy, VulnerableScript.new(3))
	# When：状态效果通过动作施加到目标身上。
	apply_strength.execute(null)
	apply_more_strength.execute(null)
	apply_vulnerable.execute(null)
	apply_more_vulnerable.execute(null)
	# Then：力量按强度叠加，易伤按持续时间叠加，并能生成策划可读的摘要。
	assert_eq(player.get_power("strength").amount, 5)
	assert_eq(enemy.get_power("vulnerable").duration, 5)
	assert_true(player.power_summary_text().contains("力量 5"))
	assert_true(enemy.power_summary_text().contains("易伤 5"))


func test_strength_increases_attack_damage() -> void:
	# Given：玩家拥有 2 点力量，敌人有 20 点生命。
	var player = PlayerScript.new([])
	var enemy = EnemyScript.new(20, "测试敌人", 0)
	player.add_power(StrengthScript.new(2))
	# When：玩家执行一次基础 6 点伤害的攻击。
	var action = CombatActionsScript.AttackAction.new(player, enemy, 6, null)
	action.execute(null)
	# Then：敌人实际损失 8 点生命。
	assert_eq(enemy.hp, 12)


func test_vulnerable_increases_damage_taken() -> void:
	# Given：敌人拥有 2 回合易伤。
	var player = PlayerScript.new([])
	var enemy = EnemyScript.new(20, "测试敌人", 0)
	enemy.add_power(VulnerableScript.new(2))
	# When：玩家对敌人造成 6 点基础伤害。
	var action = CombatActionsScript.AttackAction.new(player, enemy, 6, null)
	action.execute(null)
	# Then：易伤使敌人实际损失 9 点生命。
	assert_eq(enemy.hp, 11)
```

Also add the new test file to `.gutconfig.json`:

```json
{
  "tests": [
    "res://scripts/stm/tests/core_skeleton_test.gd",
    "res://scripts/stm/tests/test_battle_debug_scene.gd",
    "res://scripts/stm/tests/test_fixed_battle_fixture.gd",
    "res://scripts/stm/tests/test_powers_v1.gd"
  ],
  "should_exit": true,
  "should_exit_on_success": true,
  "log_level": 2
}
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: FAIL because `res://scripts/stm/powers/strength.gd`, `res://scripts/stm/powers/vulnerable.gd`, or `StmCombatActions.ApplyPowerAction` does not exist.

- [ ] **Step 3: Write minimal implementation**

Create `scripts/stm/powers/power.gd`:

```gdscript
class_name StmPower
extends RefCounted

const STACK_INTENSITY := "intensity"
const STACK_DURATION := "duration"

var power_id: String = ""
var display_name: String = ""
var amount: int = 0
var duration: int = -1
var stack_type: String = STACK_INTENSITY
var is_buff: bool = false
var owner = null


func _init(p_amount: int = 0, p_duration: int = -1) -> void:
	amount = int(p_amount)
	duration = int(p_duration)


func stack_with(other) -> void:
	if other == null:
		return
	if stack_type == STACK_DURATION:
		duration = max(0, duration) + max(0, int(other.duration))
		return
	amount += int(other.amount)


func modify_damage_dealt(value: int, _target = null, _card = null) -> int:
	return value


func modify_damage_taken(value: int, _source = null, _card = null) -> int:
	return value


func modify_block_gained(value: int, _source = null, _card = null) -> int:
	return value


func on_turn_start() -> void:
	pass


func on_turn_end() -> void:
	if duration > 0:
		duration -= 1


func is_expired() -> bool:
	return duration == 0


func summary_text() -> String:
	var visible_value := amount
	if stack_type == STACK_DURATION:
		visible_value = duration
	return "%s %d" % [display_name, visible_value]
```

Create `scripts/stm/powers/strength.gd`:

```gdscript
class_name StmStrengthPower
extends StmPower


func _init(p_amount: int = 0) -> void:
	power_id = "strength"
	display_name = "力量"
	amount = max(0, int(p_amount))
	duration = -1
	stack_type = STACK_INTENSITY
	is_buff = true


func modify_damage_dealt(value: int, _target = null, _card = null) -> int:
	return max(0, int(value) + amount)
```

Create `scripts/stm/powers/vulnerable.gd`:

```gdscript
class_name StmVulnerablePower
extends StmPower


func _init(p_duration: int = 0) -> void:
	power_id = "vulnerable"
	display_name = "易伤"
	amount = 0
	duration = max(0, int(p_duration))
	stack_type = STACK_DURATION
	is_buff = false


func modify_damage_taken(value: int, _source = null, _card = null) -> int:
	return max(0, int(floor(float(value) * 1.5)))
```

Modify `scripts/stm/entities/creature.gd` by keeping the existing properties and adding these methods after `gain_block()`:

```gdscript
func add_power(power) -> void:
	if power == null:
		return
	if not ("power_id" in power):
		return
	var power_id_text := str(power.power_id)
	if power_id_text.is_empty():
		return
	var existing = get_power(power_id_text)
	if existing != null:
		existing.stack_with(power)
		existing.owner = self
		return
	power.owner = self
	powers.append(power)


func get_power(power_id_text: String):
	for power in powers:
		if power != null and "power_id" in power and str(power.power_id) == power_id_text:
			return power
	return null


func has_power(power_id_text: String) -> bool:
	return get_power(power_id_text) != null


func remove_power(power_or_id) -> bool:
	var power_id_text := str(power_or_id)
	if typeof(power_or_id) == TYPE_OBJECT and power_or_id != null and "power_id" in power_or_id:
		power_id_text = str(power_or_id.power_id)
	for index in range(powers.size() - 1, -1, -1):
		var power = powers[index]
		if power != null and "power_id" in power and str(power.power_id) == power_id_text:
			powers.remove_at(index)
			return true
	return false


func power_summary_text() -> String:
	_prune_expired_powers()
	if powers.is_empty():
		return "无"
	var parts := PackedStringArray()
	for power in powers:
		if power != null and power.has_method("summary_text"):
			parts.append(power.summary_text())
	if parts.is_empty():
		return "无"
	return ", ".join(parts)


func modify_damage_dealt(value: int, target = null, card = null) -> int:
	var modified := max(0, int(value))
	for power in powers:
		if power != null and power.has_method("modify_damage_dealt"):
			modified = max(0, int(power.modify_damage_dealt(modified, target, card)))
	return modified


func modify_damage_taken(value: int, source = null, card = null) -> int:
	var modified := max(0, int(value))
	for power in powers:
		if power != null and power.has_method("modify_damage_taken"):
			modified = max(0, int(power.modify_damage_taken(modified, source, card)))
	return modified


func modify_block_gained(value: int, source = null, card = null) -> int:
	var modified := max(0, int(value))
	for power in powers:
		if power != null and power.has_method("modify_block_gained"):
			modified = max(0, int(power.modify_block_gained(modified, source, card)))
	return modified


func notify_turn_start() -> void:
	for power in powers:
		if power != null and power.has_method("on_turn_start"):
			power.on_turn_start()
	_prune_expired_powers()


func notify_turn_end() -> void:
	for power in powers:
		if power != null and power.has_method("on_turn_end"):
			power.on_turn_end()
	_prune_expired_powers()


func _prune_expired_powers() -> void:
	for index in range(powers.size() - 1, -1, -1):
		var power = powers[index]
		if power == null:
			powers.remove_at(index)
		elif power.has_method("is_expired") and power.is_expired():
			powers.remove_at(index)
```

Modify `scripts/stm/actions/combat_actions.gd` by adding `card` to `AttackAction` and creating `ApplyPowerAction`:

```gdscript
class AttackAction:
	extends RefCounted

	var source
	var target
	var damage: int
	var card

	func _init(p_source, p_target, p_damage: int, p_card = null) -> void:
		source = p_source
		target = p_target
		damage = p_damage
		card = p_card

	func execute(_game_state = null):
		if target == null:
			return StmTypes.TerminalResult.NONE
		var final_damage := max(0, int(damage))
		if source != null and source.has_method("modify_damage_dealt"):
			final_damage = source.modify_damage_dealt(final_damage, target, card)
		if target != null and target.has_method("modify_damage_taken"):
			final_damage = target.modify_damage_taken(final_damage, source, card)
		final_damage = max(0, int(final_damage))
		if target.has_method("take_damage"):
			target.take_damage(final_damage, source, card)
		elif "hp" in target:
			target.hp -= final_damage
		return StmTypes.TerminalResult.NONE
```

Update `EnemyAttackAction.execute()`:

```gdscript
	func execute(_game_state = null):
		var attack = AttackAction.new(enemy, player, damage, null)
		return attack.execute()
```

Add this class after `GainBlockAction`:

```gdscript
class ApplyPowerAction:
	extends RefCounted

	var target
	var power

	func _init(p_target, p_power) -> void:
		target = p_target
		power = p_power

	func execute(_game_state = null):
		if target == null or power == null:
			return StmTypes.TerminalResult.NONE
		if target.has_method("add_power"):
			target.add_power(power)
		return StmTypes.TerminalResult.NONE
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: PASS for existing tests and the first three powers tests.

- [ ] **Step 5: Commit**

```powershell
git add .gutconfig.json scripts/stm/actions/combat_actions.gd scripts/stm/entities/creature.gd scripts/stm/powers/power.gd scripts/stm/powers/strength.gd scripts/stm/powers/vulnerable.gd scripts/stm/tests/test_powers_v1.gd
git commit -m "feat(stm): add base power system"
```

## Task 2: 虚弱、敏捷与格挡修正

**Files:**
- Create: `scripts/stm/powers/weak.gd`
- Create: `scripts/stm/powers/dexterity.gd`
- Modify: `scripts/stm/actions/combat_actions.gd`
- Modify: `scripts/stm/cards/card.gd`
- Test: `scripts/stm/tests/test_powers_v1.gd`

- [ ] **Step 1: Write the failing test**

Extend the constants in `scripts/stm/tests/test_powers_v1.gd`:

```gdscript
const WeakScript := preload("res://scripts/stm/powers/weak.gd")
const DexterityScript := preload("res://scripts/stm/powers/dexterity.gd")
```

Append these tests:

```gdscript
func test_weak_reduces_attack_damage() -> void:
	# Given：玩家拥有 2 回合虚弱。
	var player = PlayerScript.new([])
	var enemy = EnemyScript.new(20, "测试敌人", 0)
	player.add_power(WeakScript.new(2))
	# When：玩家执行一次基础 8 点伤害的攻击。
	var action = CombatActionsScript.AttackAction.new(player, enemy, 8, null)
	action.execute(null)
	# Then：虚弱使敌人只损失 6 点生命。
	assert_eq(enemy.hp, 14)


func test_dexterity_increases_block_from_skill() -> void:
	# Given：玩家拥有 2 点敏捷。
	var player = PlayerScript.new([])
	player.add_power(DexterityScript.new(2))
	# When：玩家执行一次基础 5 点格挡动作。
	var action = CombatActionsScript.GainBlockAction.new(player, 5, player, null)
	action.execute(null)
	# Then：玩家实际获得 7 点格挡。
	assert_eq(player.block, 7)
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: FAIL because `weak.gd`, `dexterity.gd`, or the new `GainBlockAction` constructor signature is missing.

- [ ] **Step 3: Write minimal implementation**

Create `scripts/stm/powers/weak.gd`:

```gdscript
class_name StmWeakPower
extends StmPower


func _init(p_duration: int = 0) -> void:
	power_id = "weak"
	display_name = "虚弱"
	amount = 0
	duration = max(0, int(p_duration))
	stack_type = STACK_DURATION
	is_buff = false


func modify_damage_dealt(value: int, _target = null, _card = null) -> int:
	return max(0, int(floor(float(value) * 0.75)))
```

Create `scripts/stm/powers/dexterity.gd`:

```gdscript
class_name StmDexterityPower
extends StmPower


func _init(p_amount: int = 0) -> void:
	power_id = "dexterity"
	display_name = "敏捷"
	amount = max(0, int(p_amount))
	duration = -1
	stack_type = STACK_INTENSITY
	is_buff = true


func modify_block_gained(value: int, _source = null, _card = null) -> int:
	return max(0, int(value) + amount)
```

Replace `GainBlockAction` in `scripts/stm/actions/combat_actions.gd`:

```gdscript
class GainBlockAction:
	extends RefCounted

	var target
	var amount: int
	var source
	var card

	func _init(p_target, p_amount: int, p_source = null, p_card = null) -> void:
		target = p_target
		amount = p_amount
		source = p_source
		card = p_card

	func execute(_game_state = null):
		if target == null:
			return StmTypes.TerminalResult.NONE
		var final_block := max(0, int(amount))
		if target.has_method("modify_block_gained"):
			final_block = target.modify_block_gained(final_block, source, card)
		final_block = max(0, int(final_block))
		if target.has_method("gain_block"):
			target.gain_block(final_block)
		elif "block" in target:
			target.block += final_block
		return StmTypes.TerminalResult.NONE
```

Modify `scripts/stm/cards/card.gd` inside `on_play()`:

```gdscript
func on_play(_game_state, targets := []) -> Array:
	var actions: Array = []
	if block > 0 and _game_state != null and _game_state.player != null:
		actions.append(StmCombatActions.GainBlockAction.new(_game_state.player, block, _game_state.player, self))
	if damage > 0 and targets.size() > 0:
		var source = null
		if _game_state != null:
			source = _game_state.player
		actions.append(StmCombatActions.AttackAction.new(source, targets[0], damage, self))
	return actions
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: PASS for all current tests.

- [ ] **Step 5: Commit**

```powershell
git add scripts/stm/actions/combat_actions.gd scripts/stm/cards/card.gd scripts/stm/powers/weak.gd scripts/stm/powers/dexterity.gd scripts/stm/tests/test_powers_v1.gd
git commit -m "feat(stm): apply combat power modifiers"
```

## Task 3: 测试卡牌 Bash、Inflame、Shrug It Off

**Files:**
- Create: `scripts/stm/cards/test/bash.gd`
- Create: `scripts/stm/cards/test/inflame.gd`
- Create: `scripts/stm/cards/test/shrug_it_off.gd`
- Test: `scripts/stm/tests/test_powers_v1.gd`

- [ ] **Step 1: Write the failing test**

Extend constants in `scripts/stm/tests/test_powers_v1.gd`:

```gdscript
const GameStateScript := preload("res://scripts/stm/engine/game_state.gd")
const StrikeScript := preload("res://scripts/stm/cards/test/strike.gd")
const BashScript := preload("res://scripts/stm/cards/test/bash.gd")
const InflameScript := preload("res://scripts/stm/cards/test/inflame.gd")
const ShrugItOffScript := preload("res://scripts/stm/cards/test/shrug_it_off.gd")
```

Append these tests:

```gdscript
func test_bash_deals_damage_and_applies_vulnerable() -> void:
	# Given：玩家准备打出 Bash，敌人有 20 点生命且没有易伤。
	var player = PlayerScript.new([])
	var enemy = EnemyScript.new(20, "测试敌人", 0)
	var game_state = GameStateScript.new(player)
	var card = BashScript.new()
	# When：Bash 的动作被依次执行。
	for action in card.play(game_state, null, [enemy]):
		action.execute(game_state)
	# Then：敌人受到 8 点伤害，并获得 2 回合易伤。
	assert_eq(enemy.hp, 12)
	assert_eq(enemy.get_power("vulnerable").duration, 2)


func test_inflame_applies_strength_to_player() -> void:
	# Given：玩家准备打出 Inflame。
	var player = PlayerScript.new([])
	var game_state = GameStateScript.new(player)
	var card = InflameScript.new()
	# When：Inflame 的动作被执行。
	for action in card.play(game_state, null, []):
		action.execute(game_state)
	# Then：玩家获得 2 点力量。
	assert_eq(player.get_power("strength").amount, 2)


func test_shrug_it_off_gains_block_and_draws_card() -> void:
	# Given：玩家抽牌堆里有一张 Strike，并准备打出 Shrug It Off。
	var player = PlayerScript.new([])
	player.card_manager.draw_pile = [StrikeScript.new()]
	var game_state = GameStateScript.new(player)
	var card = ShrugItOffScript.new()
	# When：Shrug It Off 的动作被依次执行。
	for action in card.play(game_state, null, []):
		action.execute(game_state)
	# Then：玩家获得 8 点格挡，并抽到一张手牌。
	assert_eq(player.block, 8)
	assert_eq(player.card_manager.hand.size(), 1)
	assert_eq(player.card_manager.hand[0].card_name, "Strike")
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: FAIL because the three test card scripts do not exist.

- [ ] **Step 3: Write minimal implementation**

Create `scripts/stm/cards/test/bash.gd`:

```gdscript
class_name StmBash
extends StmCard

const VulnerableScript := preload("res://scripts/stm/powers/vulnerable.gd")


func _init() -> void:
	card_name = "Bash"
	card_type = "attack"
	card_rarity = "starter"
	target_type = "enemy_select"
	cost = 2
	base_damage = 8
	base_magic = 2
	upgrade_damage = 10
	upgrade_magic = 3
	reset_values()


func on_play(game_state, targets := []) -> Array:
	var actions := super.on_play(game_state, targets)
	if targets.size() > 0:
		actions.append(StmCombatActions.ApplyPowerAction.new(targets[0], VulnerableScript.new(magic)))
	return actions
```

Create `scripts/stm/cards/test/inflame.gd`:

```gdscript
class_name StmInflame
extends StmCard

const StrengthScript := preload("res://scripts/stm/powers/strength.gd")


func _init() -> void:
	card_name = "Inflame"
	card_type = "power"
	card_rarity = "uncommon"
	target_type = "self"
	cost = 1
	base_magic = 2
	upgrade_magic = 3
	reset_values()


func on_play(game_state, _targets := []) -> Array:
	if game_state == null or game_state.player == null:
		return []
	return [StmCombatActions.ApplyPowerAction.new(game_state.player, StrengthScript.new(magic))]
```

Create `scripts/stm/cards/test/shrug_it_off.gd`:

```gdscript
class_name StmShrugItOff
extends StmCard


func _init() -> void:
	card_name = "Shrug It Off"
	card_type = "skill"
	card_rarity = "common"
	target_type = "self"
	cost = 1
	base_block = 8
	base_magic = 1
	upgrade_block = 11
	reset_values()


func on_play(game_state, targets := []) -> Array:
	var actions := super.on_play(game_state, targets)
	if game_state != null and game_state.player != null:
		actions.append(StmCombatActions.DrawCardsAction.new(game_state.player, magic))
	return actions
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: PASS for all current tests.

- [ ] **Step 5: Commit**

```powershell
git add scripts/stm/cards/test/bash.gd scripts/stm/cards/test/inflame.gd scripts/stm/cards/test/shrug_it_off.gd scripts/stm/tests/test_powers_v1.gd
git commit -m "feat(stm): add power test cards"
```

## Task 4: 回合边界持续时间结算

**Files:**
- Modify: `scripts/stm/engine/combat.gd`
- Test: `scripts/stm/tests/test_powers_v1.gd`

- [ ] **Step 1: Write the failing test**

Extend constants in `scripts/stm/tests/test_powers_v1.gd`:

```gdscript
const CombatScript := preload("res://scripts/stm/engine/combat.gd")
```

Append this test:

```gdscript
func test_power_duration_ticks_at_turn_boundaries() -> void:
	# Given：敌人拥有 2 回合易伤，战斗即将执行敌人回合。
	var player = PlayerScript.new([])
	var enemy = EnemyScript.new(20, "测试敌人", 0)
	var combat = CombatScript.new([enemy], "test")
	var game_state = GameStateScript.new(player)
	enemy.add_power(VulnerableScript.new(2))
	# When：敌人回合被执行一次。
	combat.execute_enemy_turn(game_state)
	# Then：敌人身上的易伤在敌人回合结束时减少 1 回合。
	assert_eq(enemy.get_power("vulnerable").duration, 1)
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: FAIL because `StmCombat.execute_enemy_turn()` has not notified enemy powers at turn boundaries.

- [ ] **Step 3: Write minimal implementation**

Modify `scripts/stm/engine/combat.gd`.

In `start_player_turn(game_state)`, call player turn start notifications before energy and draw:

```gdscript
func start_player_turn(game_state) -> void:
	combat_state.combat_turn += 1
	combat_state.reset_turn_info()
	combat_state.current_phase = "player_start"
	if game_state.player == null:
		return
	if game_state.player.has_method("notify_turn_start"):
		game_state.player.notify_turn_start()
	game_state.player.energy = game_state.player.max_energy
	game_state.add_action(StmCombatActions.DrawCardsAction.new(game_state.player, game_state.player.draw_count))
	game_state.drive_actions()
```

In `execute_player_end(game_state)`, notify player powers after discarding hand:

```gdscript
func execute_player_end(game_state) -> void:
	if game_state.player == null or game_state.player.card_manager == null:
		return
	if game_state.player.card_manager.has_method("discard_hand"):
		game_state.player.card_manager.discard_hand()
	if game_state.player.has_method("notify_turn_end"):
		game_state.player.notify_turn_end()
	combat_state.current_phase = "enemy_turn"
```

In `execute_enemy_turn(game_state)`, notify each living enemy at start and end of its action block:

```gdscript
func execute_enemy_turn(game_state):
	if game_state.player == null:
		return _result_none()
	for enemy in enemies:
		if enemy == null:
			continue
		if enemy.has_method("is_dead") and enemy.is_dead():
			continue
		if enemy.has_method("notify_turn_start"):
			enemy.notify_turn_start()
		if enemy.has_method("determine_next_intention"):
			enemy.determine_next_intention()
		var intention_actions: Array = []
		if enemy.has_method("execute_intention"):
			var produced_actions = enemy.execute_intention(game_state, self)
			if produced_actions is Array:
				intention_actions = produced_actions
		if intention_actions.size() > 0:
			game_state.add_actions(intention_actions)
		else:
			var damage: int = 0
			if enemy.has_method("get_intended_damage"):
				damage = int(enemy.get_intended_damage())
			elif "intent_damage" in enemy:
				damage = int(enemy.intent_damage)
			elif "damage" in enemy:
				damage = int(enemy.damage)
			if damage > 0:
				game_state.add_action(StmCombatActions.EnemyAttackAction.new(enemy, game_state.player, damage))
		if enemy.has_method("end_turn"):
			enemy.end_turn(game_state, self)
		if enemy.has_method("notify_turn_end"):
			enemy.notify_turn_end()
	var result = game_state.drive_actions()
	combat_state.current_phase = "player_start"
	return result if typeof(result) == TYPE_INT else _result_none()
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: PASS for all current tests.

- [ ] **Step 5: Commit**

```powershell
git add scripts/stm/engine/combat.gd scripts/stm/tests/test_powers_v1.gd
git commit -m "feat(stm): tick power durations at turn boundaries"
```

## Task 5: 策划调试场景显示状态效果

**Files:**
- Modify: `scripts/stm/debug/battle_debug_scene.gd`
- Modify: `scripts/stm/tests/test_battle_debug_scene.gd`

- [ ] **Step 1: Write the failing test**

Extend constants in `scripts/stm/tests/test_battle_debug_scene.gd`:

```gdscript
const StrengthScript := preload("res://scripts/stm/powers/strength.gd")
const VulnerableScript := preload("res://scripts/stm/powers/vulnerable.gd")
```

Append this test:

```gdscript
func test_debug_scene_displays_player_and_enemy_powers() -> void:
	# Given：策划调试场景中，玩家有力量，敌人有易伤。
	var scene = _instantiate_debug_scene()
	assert_not_null(scene)
	if scene == null:
		return
	scene.game_state.player.add_power(StrengthScript.new(2))
	scene.enemy.add_power(VulnerableScript.new(3))
	# When：调试场景刷新显示。
	scene._refresh_display()
	# Then：界面能同时看到玩家和敌人的状态效果。
	assert_eq(_label_text(scene, "Layout/Metrics/PlayerPowersLabel"), "玩家状态效果：力量 2")
	assert_eq(_label_text(scene, "Layout/EnemyPanel/EnemyPowersLabel"), "敌人状态效果：易伤 3")
```

Also extend `test_debug_scene_fixture_failure_clears_old_display_and_disables_all_actions()` with:

```gdscript
	assert_eq(_label_text(scene, "Layout/Metrics/PlayerPowersLabel"), "玩家状态效果：无")
	assert_eq(_label_text(scene, "Layout/EnemyPanel/EnemyPowersLabel"), "敌人状态效果：无")
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: FAIL because `PlayerPowersLabel` and `EnemyPowersLabel` do not exist.

- [ ] **Step 3: Write minimal implementation**

Modify `scripts/stm/debug/battle_debug_scene.gd`.

Add variables near the existing label variables:

```gdscript
var player_powers_label: Label
var enemy_powers_label: Label
```

In `_build_ui()`, add player powers to `metrics` after `block_label`:

```gdscript
	player_powers_label = _new_label("PlayerPowersLabel")
	metrics.add_child(player_powers_label)
```

In `_build_ui()`, add enemy powers to `enemy_panel` after `enemy_attack_label`:

```gdscript
	enemy_powers_label = _new_label("EnemyPowersLabel")
	enemy_panel.add_child(enemy_powers_label)
```

In `_show_no_combat_display()`, add:

```gdscript
	if player_powers_label != null:
		player_powers_label.text = "玩家状态效果：无"
	if enemy_powers_label != null:
		enemy_powers_label.text = "敌人状态效果：无"
```

In `_refresh_display()`, add:

```gdscript
	player_powers_label.text = _power_text("玩家状态效果", player)
	enemy_powers_label.text = _power_text("敌人状态效果", enemy)
```

Add this helper near `_enemy_attack_text()`:

```gdscript
func _power_text(title: String, creature) -> String:
	if creature == null:
		return "%s：无" % title
	if creature.has_method("power_summary_text"):
		return "%s：%s" % [title, creature.power_summary_text()]
	return "%s：无" % title
```

- [ ] **Step 4: Run test to verify it passes**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: PASS for all current tests.

- [ ] **Step 5: Commit**

```powershell
git add scripts/stm/debug/battle_debug_scene.gd scripts/stm/tests/test_battle_debug_scene.gd
git commit -m "feat(debug): show combat powers"
```

## Task 6: 最终安全自检与整体验证

**Files:**
- Review: all modified files from Tasks 1-5

- [ ] **Step 1: Run full GUT suite**

Run:

```powershell
& "C:\Users\User\Desktop\Godot_v4.6.2-stable_win64_console.exe" --headless --path "C:\Users\User\Documents\GitHub\Godot-Game" -s addons/gut/gut_cmdln.gd
```

Expected: PASS, including existing调试场景测试、固定战斗夹具测试、核心骨架测试、状态效果 v1 测试。

- [ ] **Step 2: Scan for ambiguous placeholders**

Run:

```powershell
rg -n "TB[D]|TO[D]O|implement\s+later|fill\s+in\s+details|Add\s+appropriate\s+error\s+handling|Write\s+tests\s+for\s+the\s+above|Similar\s+to\s+Task" scripts/stm docs/superpowers/plans/2026-05-25-powers-v1.md
```

Expected: no matches.

- [ ] **Step 3: Review safety, boundary, dependency clarity**

Check these points explicitly:

- `slay-the-model-main/` has no diff.
- No network dependency was added.
- No new Godot addon was added.
- State effects are plain `RefCounted` scripts under `scripts/stm/powers/`.
- Combat math clamps final damage and block to non-negative integers.
- Duration powers are pruned when `duration == 0`.
- Debug scene reads `power_summary_text()` and does not own combat rules.
- Test-only cards live under `scripts/stm/cards/test/`.

- [ ] **Step 4: Commit final verification notes if code changed during review**

If review requires a code or test edit, run full GUT again and commit:

```powershell
git add scripts/stm docs/superpowers/plans/2026-05-25-powers-v1.md .gutconfig.json
git commit -m "test(stm): verify powers v1 integration"
```

Expected: only needed if Step 3 found a real issue requiring edits.

## Self-Review

**Spec coverage**
- `Strength` / `Dexterity` / `Weak` / `Vulnerable`：Tasks 1-2.
- 状态堆叠：Task 1 `test_apply_power_stacks_intensity_or_duration`。
- 攻击与格挡数值修正：Task 2。
- `ApplyPowerAction`：Task 1。
- `Bash` / `Inflame` / `Shrug It Off`：Task 3。
- 持续时间回合边界：Task 4。
- 调试场景显示玩家和敌人状态：Task 5。
- GUT 配置和整体验证：Tasks 1 and 6。

**Placeholder scan**
- 本计划不使用待填内容，不依赖“以后补齐”的步骤。

**Type consistency**
- 状态基类统一字段：`power_id`、`display_name`、`amount`、`duration`、`stack_type`、`is_buff`、`owner`。
- `StmCreature` 查询接口统一使用 `get_power(power_id_text: String)`。
- `AttackAction.new(source, target, damage, card)` 与 `StmCard.on_play()` 传参一致。
- `GainBlockAction.new(target, amount, source, card)` 与测试、基础卡牌传参一致。
- 调试 UI 节点名与测试路径一致：`PlayerPowersLabel`、`EnemyPowersLabel`。
